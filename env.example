# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=production

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-in-production

# Database Configuration (MySQL)
DB_HOST=your-mysql-host.railway.app
DB_USER=root
DB_PASSWORD=your-database-password
DB_NAME=JetPathAirline

# OpenAI API Key (for chatbot feature)
OPENAI_API_KEY=your-openai-api-key-here

# Railway will automatically set the PORT variable
# Make sure to set these environment variables in Railway dashboard:
# - DB_HOST (from your MySQL service)
# - DB_USER (from your MySQL service)
# - DB_PASSWORD (from your MySQL service)
# - DB_NAME (should be JetPathAirline)
# - JWT_SECRET (generate a secure random string)
# - OPENAI_API_KEY (if using chatbot feature)

